<section class="profile">
  {{{ comeBack }}}
  <form class="profile__info">
    {{{ avatar }}}
    <h2 class="profile__name">{{ title }}</h2>
    <div class="mt-50">{{{ labelName }}}</div>
    <div>{{{ labelSecondName }}}</div>
    <div>{{{ labelEmail }}}</div>
    <div>{{{ labelTel }}}</div>
    <div>{{{ labelLogin }}}</div>
    <div>{{{ chatName }}}</div>
    <div class="profile__btns mt-20">
      {{{ button }}}
    </div>
  </form>
</section>
